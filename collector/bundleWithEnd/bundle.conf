{
  # bundle name
  "name": "bundleWithEnd",

  # bundle version
  "version": "1.0",

  # Keep-alive
  "keep-alive": "false",

  # bundle description
  "description": "Bundle for performance",

  # bundle description
  "manifest": {
    "entry": {
      # the entry point definition
      "entryPoint": "script/Entry.lua",

      #configuration schema, the values related to this schema will be configured in the Bundle Store as a deployment document
      "configSchema": "config/configSchema.json"
    },

    # message schema
    "messages": [
      {
        "MSG_PERFORMANCE": "config/messageSchema.json"
      }
    ]
  }
}
